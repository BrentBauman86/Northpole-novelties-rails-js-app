# <ol class="categories">
#     <% collection.arrange.each do |category, toy| %>
#         <li>
#             <!-- Category -->
#             <%= link_to category.name, category_toys_path(category) %>

#             <!-- Children -->
#             <% if category.has_children? %>
#                 <%= render partial: "category", locals: { collection: category.children } %>
#             <% end %>

#         </li>
#     <% end %>
# </ol>