<h1>Welcome to Northpole Novelties!</h1>

# <% @categories.each do |category| %>
#     <%= link_to category.name, category_toys_path(category) %>
#     <%end%>

    <% if session[:user_id] %>
    <h1><%= @user.name %></h1>
    <h2>Facebook UID: <%= @user.uid %></h2>
  <% else %>
    <%= link_to('Log in with Facebook!', '/auth/facebook') %>
  <% end %>