<h2>Toy Categories</h2>

<% @categories.each do |category| %>
  <ul>
    <div class="form-vertical">
      <h3><li><%= link_to category.name, category_path(category) %></li></h3>
    </div>
    <br></br>
  </ul>
<%end%>
