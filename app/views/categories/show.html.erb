
<div class='center'>
    <div class='border'>
        <h1><%= @category.name %></h1>
    </div>
</div>

<h2>Toy List</h2>
  <ul>
    <table class="table">
        
     <tr>
         <td>Name</td>
         <td>Quantity</td>
         <td>Rating</td>
         <td>Edit</td>
        
        <% @category.toys.each do |toy| %>
         <tr>
             <td><li><%= toy.name %></li></td>             
             <td><%= toy.quantity %></td>
             <td><%= toy.rating %></td>
             <td><%= link_to "Edit Toy", category_toy_path(@category.toys, @category) %></td> 
        </tr>
        <%end%>
    </table>
  </ul>
  <%= link_to 'build toy in category show', new_category_path %>
  <%= link_to 'build new toy', new_category_toy_path(@category.toys) %>
  <br>
  <%= link_to 'category index', categories_path %>

  