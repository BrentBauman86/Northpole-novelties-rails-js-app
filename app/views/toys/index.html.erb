<% @toys.each do |toy| %>
    <%= toy.name %>
    <%end%>

<%= button_to "Create New Toy", new_category_toy_path, method: 'get' %>

<% current_user.admin %>
      <%= link_to 'Delete Toy', category_toy_path %>
    
    <%= link_to 'Back', categories_path, class: "btn btn-primary btn-light" %></p>